git init
git status
git add .
git commit -m "Initial commit"
git log
#detect change in file if git status shows changes
#git diff filename
#to undo  git add . :-
#git rm --cached -r .

#-r flag = recursive ?? . = all files
